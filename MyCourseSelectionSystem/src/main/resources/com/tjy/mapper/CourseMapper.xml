<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tjy.mapper.CourseMapper">

<!--    更新课程信息-->
    <update id="update">
        update course
        <set>
            <if test="courseCode != null and courseCode !=''">
                course_code = #{courseCode},
            </if>

            <if test="courseName != null and courseName !=''">
                course_name = #{courseName},
            </if>

            <if test="teacherId != null and teacherId !=''">
                teacher_id = #{teacherId},
            </if>

            <if test="credit != null">
                credit = #{credit},
            </if>

            <if test="capacity != null">
                capacity = #{capacity},
            </if>

            <if test="selected != null">
                selected = #{selected},
            </if>

            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        where id = #{id}
    </update>

    <!--    批量删除课程-->
    <delete id="delete">
        delete
        from course
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

<!--    条件查询-->
    <select id="list" resultType="com.tjy.pojo.Course">
        select * from course
        <where>
            <if test="courseCode != null and courseCode != ''">
                course_code like concat('%', #{courseCode},'%')
            </if>
            <if test="courseName != null and courseName != ''">
                and course_name like concat('%', #{courseName}, '%')
            </if>
            <if test="teacherId != null">
                and teacher_id = #{teacherId}
            </if>

            <if test="minCredit != null and maxCredit != null">
                and credit between #{minCredit} and #{maxCredit}
            </if>

            <if test="minCapacity != null and maxCapacity != null">
                and capacity between #{minCapacity} and #{maxCapacity}
            </if>
            <if test="isRemaining == true">
                and selected between 0 and capacity-1
            </if>
        </where>
        order by update_time desc
    </select>

</mapper>